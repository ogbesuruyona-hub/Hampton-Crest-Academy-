# Leave empty for same-origin Vercel deployment where API routes live under /api.
# Set only when the backend is hosted on a separate domain.
VITE_BACKEND_URL=

# Payment CTAs read GET /api/membership/config from the backend.
# Do not configure Stripe secret keys in frontend variables.
